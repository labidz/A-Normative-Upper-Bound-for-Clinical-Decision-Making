# requirements.txt
# -----------------
# Python dependencies for reproducing the analysis in:
#   "A Normative Upper Bound for Clinical Decision-Making Benchmarks:
#    Symbolic Guideline Execution Achieves 75.7% Accuracy on MIMIC-IV-Ext CDM"
#
# Python version: 3.12 (tested on 3.12.3; anything >=3.10 should work)
#
# Two installation profiles are provided below.
#   (a) "Paper-exact" pins the exact versions reported in the Methods section
#       and reproduces the bootstrap CIs bit-for-bit (random seed = 42).
#   (b) "Relaxed" allows minor version drift for users who can't pin exactly;
#       accuracy is invariant to these versions but bootstrap CIs may differ
#       in the 4th decimal.
#
# Install:
#   pip install -r requirements.txt

# === (a) Paper-exact versions (as reported in Methods §3.6) ===
pandas==2.2.*
numpy==1.26.*
scipy==1.13.*
scikit-learn==1.5.*
matplotlib==3.9.*
seaborn==0.13.*
statsmodels==0.14.*
tqdm==4.66.*

# === (b) Relaxed (uncomment these and comment out above if needed) ===
# pandas>=2.0,<4.0
# numpy>=1.24,<3.0
# scipy>=1.11,<2.0
# scikit-learn>=1.3,<2.0
# matplotlib>=3.7,<4.0
# seaborn>=0.12,<1.0
# statsmodels>=0.14,<1.0
# tqdm>=4.65

# === Optional: only needed if you want to re-run the LLM evaluation framework
#     described in framework.py (not required for reproducing the paper's
#     headline results, which are all symbolic/rule-based) ===
# openai>=1.50
# anthropic>=0.40
# google-generativeai>=0.8
# transformers>=4.45
# accelerate>=1.0
# bitsandbytes>=0.44
# torch>=2.3
